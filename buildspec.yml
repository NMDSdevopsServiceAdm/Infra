version: 0.2

phases:
  install:
    commands:
    # Install Terraform
    - sudo yum install -y yum-utils shadow-utils
    - sudo yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo
    - sudo yum -y install terraform
    - terraform --version
  pre_build:
    commands:
    # Assume role
  build:
    commands:
    # tf init && tf apply autoaproove